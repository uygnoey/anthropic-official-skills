# PermissionRequest: validate test commands

Runs a command validator on `PermissionRequest` events that match `Bash(npm test*)`.

## Install

1. Copy `permissionrequest-validate-tests.json` into your hooks configuration.
2. Point `command` at your validation script.

## Source

- https://claude.com/blog/how-to-configure-hooks
